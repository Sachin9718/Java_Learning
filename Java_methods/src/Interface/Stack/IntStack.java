package Interface.Stack;

public interface IntStack {
    // The push and pop method can be used any type of stack
    // stack interface will use push and pop methoed

    void push(int item);            // To store/push the element in stack
    int pop();                      // To delete the element from stack
}
